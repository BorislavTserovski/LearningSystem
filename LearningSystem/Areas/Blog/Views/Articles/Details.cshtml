@model BlogArticleDetailsServiceModel
@{
    ViewData["Title"] = Model.Title;
}

<h2>@ViewData["Title"]</h2>

<div class="row">
    <div class="col-md-4">
        @Model.Content
    </div>
</div>
<small>By: @Model.Author Published On: @Model.PublishDate</small>

